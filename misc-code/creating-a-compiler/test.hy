let x = (10 - 2 * 3) / 2 + 3;
let y = 2;//first
//first
if (1) {
    let z = 1 + 2 + 3;
    y = 0;
    z = 1;
    exit(y);
    //exit(z);
}
elif (2) {
    exit(2);
}
elif (22) {
    exit(22);
}
elif (23) {
    exit(23);
} else {
    exit(9);
}

/*
exit(x);
*/
exit(x);

//run in 'build' directory:    rm -rf out*; cmake .. && make && ./hydro ../test.hy && ./out; echo $?
